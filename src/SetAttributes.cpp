#include "SetAttributes.h"

SetAttributes::SetAttributes(float maxIterations, float brightness, float zoom, vec2 center)
    : maxIterations(maxIterations), brightness(brightness), zoom(zoom), center(center), complexPlanePos(vec2()) {}